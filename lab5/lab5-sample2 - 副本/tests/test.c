int b = 2; 
void main() {
    int a, s;
    a = 8;
    s = 8;
    char ch = 't';
    //取地址出错
    // 有参数str时 没法添加参数&ch 反之亦是
    scanf("jhkhik",&ch);

    while(a>0 && a<=10 || a%100==10 && !a==10) {
        int sh = 10; //不会是空语句块的原因吧,确实
    }
    a -= 1; //还没有添加实际操作
    int a; 
    a = 10;
    s += a; //还没有添加实际操作
    if(-s < -10){
        printf("result is: %d\n", s);
        int b;
        b = 10;

        for(int i=0; i<b; i++) 
        {
            printf("Have fun: %d\n", i);
        }
        
    }
};